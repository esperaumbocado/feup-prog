#include <iostream>
using namespace std;

bool already(int a[],int max_search, int search){

    for(int i=0;i<max_search;i++){
        
        if (search=a[i]){
            return true;
            break;
        }
    }

    return false;
}

int first_appearance(int a[],int n, int search){

    for (int i=0;i<n;i++){
        
    }
}

int firstElementKTime(int a[], int n, int k)
    {
        int repetitions[];
        
        for (int i=0;i<n;i++){
            
            if (already(a,i,a[i])){
                
            }
            
        }
    }